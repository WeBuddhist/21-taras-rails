---
topic: hara
article: article.md
method: wiki-article-from-claims
context_packages:
  - 2-RAILS/Claims/hara.md
rails_status: draft
raw_sources_cited: []
date: 2026-08-14
status: draft
---

# Citations — hara (ཧ་ར)

## Reference map

Empty. No claim on `2-RAILS/Claims/hara.md` was cited in any facet — its
"Consensus"/"⚑ Divergences"/"Unique" sections for "Hara as an independent deity" are
all empty by finding (see that page's scope note and its "Claims reviewed, not
separately cited" section, which accounts for all 37 pooled claims). With zero facet
citations, there is nothing to resolve into a reference map, and no `article.md`
wikitext fence was drafted (Rule 1: claims-only drafting — see `article.md`'s
explanation).

## Claims used but not quoted

None. No claim entered any prose because no prose was drafted.

## Claims resolved but not incorporated

Not applicable in the usual sense (this section normally logs claims the drafter chose
to leave out for space/redundancy reasons while others carried the same point). Here
every one of the 37 pooled claims was excluded for the same structural reason — see
`2-RAILS/Claims/hara.md`'s "Claims reviewed, not separately cited" section, which is
the authoritative accounting: each of the 37 `registered_id:claim_id` entries there is
logged with a one-line note of its specific content (mantra-formula quotation,
mantra-table entry, or etymological gloss of the syllable ཧ་ར as "to seize").

## Unresolvable attestations

None — this is not a resolution failure. Every claim was found and read; the
consolidated page simply attests no facet content about Hara-the-deity to resolve,
because the corpus contains none.

## Warnings

- **No article was drafted.** `article.md` in this folder carries `status:
  not-drafted` and no wikitext fence, because drafting one would require content not
  supported by any citable claim in this vault — a direct violation of both the
  `wiki-article-from-claims` skill's Rule 1 (claims-only drafting, no parametric
  knowledge) and the vault's own citation-chain rule (`4-SYSTEM/CLAUDE.md` §10).
- **`rails_status: draft`.** `2-RAILS/Claims/hara.md` itself is `status: draft`
  (an LLM never marks its own consolidation `complete`, per vault rule). Its content is
  a completed, verified negative finding, not a partial synthesis awaiting more raw
  material — `verify_consolidation.py` returned 0 errors, 0 warnings against it.
- **Queue-entry accuracy.** The upstream queue
  (`0-INBOX/AI_translation/keyword-extraction/output/article_queue.md`, rank 67; count
  "37 (16/16)") pooled claims by surface-string match on ཧ་ར without distinguishing
  the mantra syllable from the deity name. That pooled count is fully accounted for on
  `2-RAILS/Claims/hara.md` — all 37 are mantra-related — but the queue's implicit
  premise (that this corpus discusses Hara as a deity) does not hold. A human
  contributor may want to mark this queue entry as not viable for a term-article from
  this vault's own sources, independent of Hara's general Wikipedia notability as a
  Śiva-epithet.
- **No public URLs / no network activity.** This run made no network calls and
  published nothing; only vault files listed above were written.

## Verification

Not applicable — there are no quotations in this deliverable to verify character-for-
character against `1-SOURCES/`, because no wikitext body was drafted.
