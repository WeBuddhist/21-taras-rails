Read `4-SYSTEM/Skills/toc-tree-ingest/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Ingests a pre-extracted TOC tree into a commentary file by inserting markdown headings with block IDs, in a single document-order pass.
