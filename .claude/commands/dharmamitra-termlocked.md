Read `4-SYSTEM/Skills/dharmamitra-termlocked/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Vocabulary-standardised translation through the DharmaMitra API — build a lock glossary from the track termbase, send it with every call, then mechanically verify every lock landed and re-run only the blocks where it did not. Writes to `Dharmamitra-termlocked/`, never to the zero-shot baseline track.
