Read `../Webuddhist-Skills/rails/keyword-standardize/SKILL.md` in full (the canonical skill in the shared Webuddhist-Skills repo, cloned next to this vault) and follow it for the language, grade or terms the user specifies. Run its scripts as `python3 ../Webuddhist-Skills/rails/keyword-standardize/scripts/<script>.py …` and graded-translate's as `python3 ../Webuddhist-Skills/rails/graded-translate/scripts/<script>.py …`, from this vault's root.

Skill purpose: standardise the target-language keywords (Chinese, Vietnamese, …) of a Tibetan text that has no human translation in that language. Build one evidence-backed rendering per locked Tibetan term, recorded in `<tgt>-decisions-<grade>.json`, then build the termbase, grade file, glossary and review table from it.

This vault's word lists: `0-INBOX/AI_translation/keyword-extraction-dharmamitra/<lang>/` (see its `00-INDEX.md`).
