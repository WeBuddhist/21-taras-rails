Read `4-SYSTEM/Skills/keyword-extract/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: The vocabulary-standardization pipeline — extract candidate keywords from a block-aligned English translation, map every occurrence back to the Tibetan term it renders, group variants under one canonical lemma, count quote-excluded across the corpus, score, and emit a mechanically-gated ranked article queue. Writes `2-RAILS/Keywords/`. Supersedes `english-keyword-extraction`.
