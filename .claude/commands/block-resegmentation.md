Read `4-SYSTEM/Skills/block-resegmentation/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Re-draws block boundaries in a Stage-1 segmented commentary to produce semantically coherent, citation-sized units. Runs after commentary-segmentation and after the TOC step.
