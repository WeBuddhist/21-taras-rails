Read `4-SYSTEM/Skills/lint-annotations/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Report-only annotation-convention linter: verse structure, lines per verse, block IDs, heading anchors, stray footnote digits. Never fixes anything without explicit confirmation.
