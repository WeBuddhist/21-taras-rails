Read `4-SYSTEM/Skills/gemini-keyword-extract/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Extract key terms directly from Tibetan blocks with Gemini, with per-block provenance, and verify every returned term against the block it was claimed from (VERBATIM / NORMALIZED / ABSENT). Built as the comparison arm against `keyword-extract`'s English-mediated route — `gk_compare.py` reports where the two agree and what each finds alone.
