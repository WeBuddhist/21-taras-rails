Read `4-SYSTEM/Skills/Transclusion-rootext-into-commentaries/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Transcludes root-text verses into a Tibetan commentary and fixes the blank-line spacing around each transclusion. Three deterministic stages, dry-run by default.
