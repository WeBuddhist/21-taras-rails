Read `4-SYSTEM/Skills/zeroshot-translator/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Produces a direct zero-shot translation into a target language guided by an audience profile, with no termbase. The fast path — pada-aligned, segment IDs preserved.
