Read `4-SYSTEM/Skills/wiki-article-inventory/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Determine, for every standalone article subject, whether bo.wikipedia already has the article — via title/variant lookup and Wikidata sitelinks — and save a per-subject inventory with dated wikitext snapshots, feeding the kwiki terms.yaml.
