Read `4-SYSTEM/Skills/claims-fact-check/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Fact-check a translation block by block against the whole corpus at once — every verse-aligned commentary plus the consolidated claims page — grading each finding ERROR / ⚑ tradition-specific / unsupported / OK. Vault-local (needs `2-RAILS/Claims/`). Reports only; never edits the translation.
