Read `4-SYSTEM/Skills/graded-translate/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Audience-graded, term-locked translation in three phases — build the per-grade termbase on top of `2-RAILS/Keywords/`, translate with every rendering locked, then run the mechanical drift check. The vocabulary-standardised path; `dharmamitra-translate` is the unlocked baseline.
