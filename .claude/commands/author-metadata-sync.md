Read `4-SYSTEM/Skills/author-metadata-sync/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Propagate human-curated author metadata (author, author_in_use, author_in_english) from the commentary frontmatters in 1-SOURCES/ into the raw tree-guided claims files, and report every registry surface that still disagrees.
