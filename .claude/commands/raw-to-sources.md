Read `4-SYSTEM/Skills/raw-to-sources/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Bring one raw OCR/segmentation text file into 1-SOURCES/ as a cleaned, frontmattered root-text or commentary file — the first step of the ingest chain, before any segmentation, TOC, or block IDs.
