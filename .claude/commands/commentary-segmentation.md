Read `4-SYSTEM/Skills/commentary-segmentation/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Segments an under-segmented Tibetan commentary into short, individually-citable blocks using the text's own functional signals. Runs after format-commentary, before block-ID stamping.
